import { binaryToDecimal } from "./binaryToDecimal";
import * as decimalToBinary from "./decimalToBinary";

function checkSetInBinary(decimalNumber: number): [] {
    const result = [];

    const binaryNumberFromDecimal = decimalToBinary.decimalToBinary(decimalNumber);

    const 

    return result;
}